# Bar Trivia Docker demo environment
# These are demo-only secrets — replace before any real deployment
JWT_SECRET=dev-jwt-secret-change-in-production
COOKIE_SECRET=dev-cookie-secret-change-in-production
